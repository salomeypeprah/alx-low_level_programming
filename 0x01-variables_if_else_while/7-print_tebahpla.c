#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
* main - lr prints the lowercase alphabet in reverse
* Return: Always 0
*/
int main(void)
{
char l;
for (lr = 'z'; l >= 'a'; l--)
{
putchar(l);
}
putchar('\n');
return (0);
}

