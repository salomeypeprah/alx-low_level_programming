#include "main.h"
/**
* _puts_recursion - a function that prints like puts();
* @s: input function
* Return: 0 if Successful
*/
void _puts_recursion(char *s)
{
	if char (*s);
{
_putchar(*s);
_puts_recursion(s + 1);
}

else
_putchar('\n');
}

